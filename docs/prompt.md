1. Solution step-by-step and how to present the solution:
    - Must include: sensitivity analysis, what if analysis
    - Think in a way to also maximize the exergy efficiency, energy efficiency, and heat recovery
2. Start with a fermi estimate of how much the energy savings can impact the EUR/ton unit
    1. Estimate of how much operation hub can decrese EUR/ton
    2. Estimate of how much decision hub can decrease EUR/ton
    3. Estimate of how much both can decrease EUR/ton
3. Follow up with the proof:
    1. Energy hub model:
        1. Components:
            1. Sources (elec grid, gas grid, pv, ):
            2. Converters: chp, e-boilers, industrial heat pump
            3. Loads:  (either mother high level of detail or simply 1 fixed load of elec and gas input, to ton of product output)
            4. Storage: Battery, Thermal Energy storage
        2. Buses:
            1. Electricity
            2. Fuel
            3. Heat 
        3. Sink: Process Electrical Load, Process Heat Demand.
    2. First solution with optimized operational planning using the existing utilities
        1. Using the past spot market data and existing utilities available data, model how much saving can be generated by optimizing operational dispatch through load shifting, arbitrage, etc using MILP
            1. Gas: check If the factory already has e-boiler invested
            2. Electricity: check if the factory already has PV and battery invested
        2. Explain how this solution can be implemented real time using MCP rolling horizon technique
        3. Present how much savings can be generated 
    3. Second solution using decision hub
        1. Business analysis:
            1. Where are they located: 
                1. Screenshot from google earth of an empty roofstop to add more pv capacity
                2. Estimate how much free space for PV rooftop or facade
                3. run a joint dispatch and sizing optimization
2. Repo and code structure & ideology:
    - Modular and plug & play ideology with uv or other best practice
    - Repo structure:
        - Maybe I can establish an efficient abstraction:
            - under  src/, one optimization_model.py module abstraction for the optimization model for the oemof.solph build definition and pvlib application and also one external_api.py module that retrieves the necessary csv data for the optimization from open source API like (spot price, etc.)
            - one challenge.ipynb module to run the model and conduct analysis, explanation, and justification that lives in root repo. :
                - Here in the first cell, the general .md walkthrough of how I am approaching the challenge.
                - Before the optimization solution, I present first the fermi estimate as a baseline reasoning of why the solution is considered in the first place. This is also when I will create the baseline cost to compare to the optimization results afterwards.
                - First code cell should be config that dictates what technologies should be activated in the energy system, which pv model, CHP, thermal energy storage, battery models, elec & heat demand chosen from /assets, and also config to determine whether the components are fixed or variable capacity and make also the name of the energy system configurable and changeable. I want two different energysystems, 1 for operational hub solution, and the other for decision hub solution.
                - Second code cell should be the run code (first run for operation hub).
                - Third code cell should be data analysis of the first energy system definition
                - Then a new config for a new energy system definition (decision hub), and then another run code, and then another data analysis.
                - I will also provide a sensitivity analysis section answering the what if question by running the energy system under varying assumptions
                - another a section that explains the summary of the solution and the impact of the business case to Henkel and how the solution will be actually be deployed in real case.
                - For each section, I will always then explain assumption, thoughts, and comments.
                - And then another section that bridge the first question to the second question of the challenge which is **Decide what you'd ask Henkel for in the first on-site visit.**
                    - The single most load-bearing data request.
                    - The single most load-bearing stakeholder you need 30 minutes with.
        - agents/skills for antigravity: Should I show this on the repo or should it be gitignored?
            - Python best practice
            - German energy market specialist
            - MILP optimization engineer specialist: ensure best practice, fast convergence, etc
            - Bridge agent skills that connect the energy market specialist and MILP optimization engineer
            - Solution architect career coach expert
        - ref
            - This folder is where all the references for the assumption is organized. I am not sure in what format I should store them, but maybe some should be pdf and others should be links stored in .md?
        - uv.lock
        - gitignore
        - README.md
            - Explain the repo strucutre, how the reader should navigate the repo, and why I structure the repo this way
            - Explain some technical background of stacks used in this repo
            - thought-process
                - How would I start approaching the challenge
                - The draft of the plan
                - arguments and justification why I choose my approach and solution
3. library requirement: oemof.solph, pvlib, cbc, pandas, matplotlib, other necessary libraries

